# Copy to .env and fill in your keys. Do not commit .env to GitHub.
# API_KEY is required for Alpha Vantage (Gold/Silver and Stock Daily).
# OLLAMA_API_KEY is optional; if set, AI report uses Ollama Cloud; otherwise uses local Ollama.

API_KEY=your_alpha_vantage_key_here
OLLAMA_API_KEY=
